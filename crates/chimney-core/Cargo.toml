[package]
name = "chimney-core"
version = "0.1.0"
edition = "2024"

[dependencies]
toml = { workspace = true, optional = true }
serde = { workspace = true }
thiserror = { workspace = true }

[features]
default = ["toml"]
toml = ["dep:toml"]
